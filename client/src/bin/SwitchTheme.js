{
  /* <FormControlLabel
                label={isChecked ? "Light" : "Dark."}
                sx={{
                  "& .MuiFormControlLabel-label": {
                    color: palette.text.primary,
                    fontWeight: 600,
                  },
                }}
                control={
                  <Switch checked={isChecked} onClick={() => setMode()} />
                }
              /> */
}
